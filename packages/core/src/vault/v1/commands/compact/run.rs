use std::path::Path;

use crate::errors::Result;
use crate::vault::shared::commands::CompactConfig;

pub fn run(_path: &Path, _password: &[u8], _config: Option<CompactConfig>) -> Result {
    todo!()
}
