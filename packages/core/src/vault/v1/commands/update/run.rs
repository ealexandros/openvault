use std::path::Path;

use crate::errors::Result;
use crate::vault::shared::commands::UpdateConfig;

pub fn run(_path: &Path, _password: &[u8], _config: Option<UpdateConfig>) -> Result {
    todo!()
}
